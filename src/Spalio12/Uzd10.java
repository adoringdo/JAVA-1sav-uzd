package Spalio12;

public class Uzd10 {
    public static void main(String[] args) {
        String[] names = {"Bill", "Sam", "Tom", "Lucas", "Lena", "Fiora"};
        for (int i = 0; i < names.length; i++) {
            System.out.println(names[i].toUpperCase() + "-" + names[i].length() + "-" + i);
        }
    }
}
